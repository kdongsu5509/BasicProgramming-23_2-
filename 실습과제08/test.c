#include <stdio.h>
#include <math.h>

int main()
{
    int k = 36;
    int g = sqrt(k);
    printf("%d", g);
}

int sum = 0;
for (int i = 1; i * i <= n; i++)
{
    if (n == 1)
    {
        sum = 1;
    }
    else if (i *i = n)
    {
        sum = sum + i;
    }
    else
    {
        sum = sum + i;
        sum = sum + (sum / i);
    }
}