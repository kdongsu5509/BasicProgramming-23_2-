C 의 마법사

Call by value

Pointer : storage of adress of memort that indicate memory adress


함수내에서 여러가지 결과를 출력할 수 있다.
-변수가 스택영역에서 제거될려면 ->  변수의 스택이 바뀌어야 한다.

포인터를 이용하면 함수 내에서 외부 함수에 접근 가능 / 외부 함수에 값 할당도 가능 / 그 함수는 아직 영역 삭제 안됨 -> 값 변경처럼 느껴짐

배열의 이름 -> 포인터 변수

포인터를 이용한 연산에는 자연스레 형식 매개변수가 많을 수 밖에 없다!!
====> 형식 매개변수와 실 매개변수간의 괴리가 발생하는 경우가 많더라~~. 그래서 실제 어떤 함수를 구현할 때
형식 매개변수가 많고, 실 매개변수의 이름과 헷갈리면 --> 주석을 통해서 각각 매칭을 좀 시키자 
예) 형식 매개변수 x는 실 매개변수 y에 binding되어 있다. 또한 형식 매개변수 x의 자료형은 ~~type이다.
--> x === y ; string // x === y ; int // x === y ; structure etc.